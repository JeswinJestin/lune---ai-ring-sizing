import { useEffect } from 'react';

export const useSmoothScroll = () => {
    useEffect(() => {
        // Smooth scroll disabled for better performance and user experience
        // Native browser scrolling is used instead
    }, []);
};
